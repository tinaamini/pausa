import 'package:flutter/material.dart';

class AppColors {
  // background
  static const backGroundLight= Color(0xFFFFF8EA);
  static const backGroundDark = Color(0xFF1A1440);

  //
  static const navigation = Color(0xFF7C7A7A);


  // Primary Purple
  static const primary = Color(0xFF7C6FE0);
  static const primaryLight = Color(0xFF9B91E8);
  static const primaryDark = Color(0xFF5A4FC4);

  // Dark Theme

  static const darkBorder = Color(0xFF2E2A4A);

  // Light Theme

  static const lightBorder = Color(0xFFFFFFFF);

  // Accent
  static const accent = Color(0xFFFF7B54);      // streak fire
  static const success = Color(0xFF4CAF50);
  static const warning = Color(0xFFFFC107);
  static const error = Color(0xFFE53935);

  // Text
  static const textDark = Color(0xFFFFFFFF);
  static const textDarkSecondary = Color(0xFFB0A8D0);
  static const textLight = Color(0xFF1A1440);
  static const textLightSecondary = Color(0xFF6B5FA0);
}